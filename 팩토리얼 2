#include <stdio.h>

long long int factorial(int n) {
    if (n <= 1) return 1;
    return n * factorial(n - 1); // f(n)=n*f(n-1)이용
}

int main() {
    int num;
    scanf("%d", &num);
    printf("%lld\n", factorial(num));
    return 0;
}
